# Model Artifact

Place your trained model file here, e.g. `Alzheimer_Detection_model.h5`.

Note: Large binaries are ignored by `.gitignore`. Use Git LFS or provide download instructions. Update `backend/config.py` if your filename or path differs.
